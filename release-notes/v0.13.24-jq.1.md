# Proma fork v0.13.24-jq.1

> 基线：官方 package.json version 0.13.24（upstream `4f184c18`）。本版为「跟随官方发版」——同步官方 v0.13.16 → 最新共 31 个提交（含官方 v0.13.22 release），fork 自有改动无新增。
> 不签名、手动下载安装。

## 同步官方（可见功能 / 增强）

- **proma CLI 集成**：proma 命令行编译为自包含二进制并打进桌面构建；session-cleaner 等 skill 薄壳化为 CLI 封装，引用会话改用 CLI 渐进读取（#990/#991/#992/#994）
- **会话处理核心重构**：抽取 `@proma/session-core` 作为会话处理唯一真源（#988）
- **WPS 文档附件支持**（#979）
- **长文本粘贴自动转附件 toggle**（#987）
- agent stream 注入支持 `/skill` `#mcp` `&session` 结构化元数据（#965）
- 侧栏 automation / skills 入口支持再次点击收起（#972）
- 批量委派时父会话不消失（#985）
- queueMessage 传 workspaceSlug（#997）

## 修复 / 稳定性

- 修复排队引用（queued reference）持久化（#998）
- 修复侧栏子会话可见性（#1004 / #1001）
- 创建会话后自动显示所在工作区（#1000）
- 移动会话对话框用会话自身工作区做排除过滤（#983）
- 修复 diff 面板折叠时 tab 栏文字竖排换行（#975）
- mention Esc 关弹窗竞态修复（#984）
- Windows 下隐藏 Claude 子进程窗口

## UI / 主题打磨

- 暗色模式表层对比度提升（#1002）
- 多处 theme / 护眼配色微调、WindowControls 拖拽区与对齐修复
- Markdown TOC 手动开闭等若干编辑器细节

## fork 自有改动

- 无新功能提交。仅同步上游 + `package.json` version → `0.13.24-jq.1`。
- fork-local 覆盖（electron-builder / github-release-service owner = ai-jiaqian、release.yml 未签名守卫、AboutSettings 链接、README 安装段）保持在位，上游本批均未触碰。

## 安装

不签名手动下载。macOS 首次打开需右键「打开」绕过 Gatekeeper，或终端跑一键安装脚本：

```bash
curl -fsSL https://raw.githubusercontent.com/ai-jiaqian/Proma/main/scripts/install.sh | bash
```
