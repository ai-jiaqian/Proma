# Proma `v0.15.11-jq.1` Release Notes

> 基线：上游 `v0.15.11`（ErlichLiu/Proma，2026-07-23 发布）+ 后续 8 个提交（至 `3e45726a`）。fork 后缀 `-jq.1`。
> 本版 = 同步官方 v0.15.7→v0.15.11 及后续（累计 39 提交）+ fork 自有 revert（剔除推广标识）。

## 二开自有（fork 自有改动）
- **剔除官方 #1275「Made-with: Proma」推广标识**（fork 自有 revert）— 官方 v0.15.11 新增在 git 提交 trailer / PR footer 默认注入 `Made-with: Proma` 推广链接。fork 不给用户的 commit/PR 注入第三方推广内容，故剔除（删除 `agent-git-attribution.ts` 及编排器/会话/设置里的注入与开关）。其余官方改动全保留。

## 同步官方（v0.15.7 → v0.15.11 及后续 8 提交）

### 新功能
- **支持 Claude Opus 5 1M 上下文**（#1308）
- **统一 Pi reasoning 能力**（#1303）— 新增 reasoning-profile，重构 thinking 级别
- **会话创建入口收敛至项目层**（#1252）
- **OpenCode Go 订阅渠道**（#1263）
- **Pi 当前会话上下文 compaction 工具**（#1246）
- **macOS 终端文件夹动作**（#1254）
- **模型渠道 tooltip**（#1264）
- **GPT-5 能力外推**（#1250）
- **第三方渠道风险确认**（#1253）
- **工具活动中识别 Skill 读取**（#1301）

### 运行时升级
- **Electron 39.5.1 → 43.2.0**（#1305）+ CI Node 20→22

### 修复与打磨
- **Pi HTTP chunked/SSE 流中断原生重试**（#1267）— 网络抖动不再断流
- **便签图片 UX 与 Markdown 持久化改进**（#1183）
- **本地 Markdown 文件链接渲染为 chips**（#1234）
- **便签复制段落间距规范化**（#1274）
- **Pi 终端错误时保留 partial 输出**（#1268）
- **OpenCode Kimi K3 上下文窗口修复**（#1271）
- **Windows 默认 Shell 改为 Git Bash**（#1272）
- **记住默认 reasoning effort**（#1241）
- **委派会话完成通知静音**（#1251）
- **Excel 预览每 sheet 显示 200 行**（#1294）
- Claude catalog 模型匹配放宽（#1307）、Pi 委派继承当前模型（#1306）、PDF skill frontmatter 加载（#1302）、Pi compaction 后继续任务（#1300）
- 侧边栏项目分区对齐（#1259）、Codex OAuth 语义标题（#1248）等

## fork-local 覆盖（全部保留）
electron-builder `owner: ai-jiaqian`、github-release-service `owner: 'ai-jiaqian'`、release.yml 未签名守卫（`CSC_IDENTITY_AUTO_DISCOVERY` ×2）、AboutSettings 链接指向 fork、PROJECT.md 自动注入 + `KNOWLEDGE_MANAGEMENT_SECTION` 提示词常量。

## 安装
- macOS（Apple Silicon / Intel）：下载对应 DMG，拖入 Applications。未签名，首次打开需在「系统设置 → 隐私与安全性」允许。
- 一键脚本：`curl -fsSL https://raw.githubusercontent.com/ai-jiaqian/Proma/main/scripts/install.sh | bash`
