# Proma v0.16.1-jq.1

> 基线：官方 v0.16.1（2026-07-30 发布）。fork 后缀 .1：本次为纯同步官方，无新增自有改动。
> 前置：需先完成同步（merge upstream/main，解 6 文件冲突），步骤见 `commands-v0.16.1-jq.1.md`。

## 同步官方（v0.15.11 → v0.16.1，44 个提交）

### 新功能

- **本地规划工作区**：todo 待办 + 日历日程（#1323），日历周视图增强（#1333），规划窗口快捷键（#1344），从 todo 一键起 Agent（#1347），会话里可引用 todos 和日历事件（#1329）
- **设置工作区重设计**（#1331），设置头简化（#1341）
- **快捷键地图**（#1318）：一张图看全所有快捷键
- 统一项目与会话的 Files 面板（#1322）
- 会话 hover 预览开关，移除自动文件定位（#1335）
- Agent 有草稿时显示停止控件（#1342）
- 支持 Claude Opus 5 1M 上下文（#1308）
- 统一 Pi reasoning 能力（#1303）
- 工具活动里识别 Skill 读取（#1301）

### 修复

- Pi 内核：80% 上下文触发自动 compaction（#1321）、compaction 后继续任务/恢复进度（#1300/#1316）、原生重试生命周期加固（#1319）、损坏的上游流式响应重试（#1317）、委派继承当前模型（#1306）
- Agent：运行中允许切换模型（#1325）、委派会话 UI 生命周期同步（#1337）、统一命令菜单加固（#1326）、runtime/thinking 切换延迟生效（避免打断当前会话）
- 移除 brainstorming skill（#1332）
- 飞书新 bot 会话镜像恢复（#1346）
- Codex 标题生成串行化（#1328）
- 侧栏：置顶会话不限数（#1315）、新会话快捷键紧凑化（#1345）
- 引用选中内容后聚焦目标会话（#1304）
- Excel 预览每 sheet 显示 200 行（#1294）
- todo 计数排除已完成项（#1343）

### 底层

- Electron 运行时 39.5.1 → **43.2.0**（#1305，CI Node 20→22）
- pi-ai / pi-coding-agent patch 升级至 0.82.1

### ⚠️ 值得注意的官方动向

- **#1327 强化 Pi 内核推荐与 Claude 下线提示**：官方在推动用户从 Claude 内核迁往 Pi。fork 目前跟随官方默认（新会话默认 Pi），Claude 用户会看到迁移提示。

## fork 自有（延续自上一版，无新增）

- Codex OAuth 登录/刷新走系统代理（v0.15.11-jq.2 已带）
- 剔除官方 Made-with: Proma 推广标识（v0.15.11-jq.1 起）

## 安装

不签名 DMG，手动下载安装。见 release 附件。
