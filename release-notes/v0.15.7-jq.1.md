# Proma v0.15.7-jq.1 发版说明（草稿 · 待人工拍板）

基线：官方 v0.15.7（package.json 0.15.7），fork 后缀 -jq.1。
内容 = 同步官方 30 提交（v0.15.3-jq.2 之后 v0.15.5/v0.15.6/v0.15.7 三档的内容）。
fork 侧本版无自有新功能，纯同步。

## ⚠️ 同步时需注意的行为变更

- **#1221 新会话默认使用 Pi runtime** —— 官方把新建会话的默认 Agent 运行时切到了 Pi（实验性）。fork 用户习惯 Claude 内核，同步时建议确认/保留 Claude 为默认（或在 release notes 里明确告知用户变化）。
- **#1223 Agent 默认直接执行**（default to direct execution）——默认权限行为变化。
- **Pi SDK 回滚 0.80.10 → 0.80.9**（#1219，官方主动回滚，解 package.json 冲突时取上游 0.80.9）。

## 同步官方（30 提交）

### 新功能
- 会话列表星标标记（未星标时仅 hover 显示）#1232
- 新增通义千问 token plan 渠道 #1210
- Agent 实时任务进度浮层（live task progress overlay）#1205
- 新会话默认 Pi runtime #1221（见上方注意）
- Agent 默认开启 thinking #1220

### 修复
- Pi：委派会话直接运行 #1237、Pi 终端流中断安全恢复 #1236、重试时防重复委派 #1209、Doubao developer role 映射 system #1225
- Codex OAuth：持久化刷新后的凭据 #1217、恢复运行时鉴权 #1213、GPT-5.4/5.5/5.6 上下文窗口修正 272K/372K #1231/#1229/#1227（含 #1222 的 revert #1226）
- Agent：重试前清除残留错误 #1228、默认直接执行 #1223
- 渠道：qwen/小米 token plan 生成标题时省略 thinking 字段 #1233、GLM-5.2 输出 token 上限 #1206、agent core 兼容性说明 #1207
- UI：超宽 Mermaid 图左侧内容可滚动 #1224
- 其他：移除 process display 选项 #1211、清理 pre-0.15 旧 release notes

### 依赖
- Pi SDK 0.80.10 → 0.80.9（官方回滚 #1219）→ merge 后必须 `bun install`

## fork 自有内容（延续保留）
- fork 一键安装/更新脚本（install.sh / install.ps1）
- 发布与更新源指向 ai-jiaqian/Proma（electron-builder owner、github-release-service、AboutSettings、README）
- 未配置 MAC_CERTS 时跳过签名（未签名发版守卫）
- 内置默认 Skill：proma-dev-context / proma-research-context
- 工作区 PROJECT.md 自动注入与知识管理提示词

## 安装
见 release 页面 dmg（不签名，需在「系统设置 → 隐私与安全性」中允许打开）。
终端一键：`curl -fsSL https://raw.githubusercontent.com/ai-jiaqian/Proma/main/scripts/install.sh | bash`
