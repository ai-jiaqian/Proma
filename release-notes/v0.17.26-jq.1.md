# v0.17.26-jq.1 发版说明（草稿，2026-08-14 生成）

> 基线：官方 v0.17.26（published 08-13 23:44 GMT+8，tag=`db94285a`），同步 upstream/main 自 `751d25ce` 至 `db94285a`（28 个提交）。
> fork 自 v0.17.16-jq.1 起无自有提交，本版全部为同步官方内容。

## 同步官方 v0.17.26

### 受管浏览器持续完善

- 支持真实 popup 与静默下载，页面打开新窗口/下载文件不再被误拦截（#1616）。
- 放开受限 URL 导航（#1605）；忽略迟到的旧标签导航事件（#1592）。
- 方向键/翻页键携带键码，可触发页面默认滚动（#1597）。
- 移除浏览器文件面板收起提示 toast（#1606）。
- in-app-browser Skill 补充 SPA 内部滚动容器的操作与验证指引，版本 1.0.11 → 1.0.12。

### 性能优化（本批重头）

- 减少后台流式渲染抖动（#1620）、隔离输入框与历史渲染（#1613）、降低输入渲染抖动（#1591）。
- 移除历史滚动扫描（#1600）、优化历史选区引用（#1618）。
- 长会话/长草稿场景应明显更流畅。

### 自动化（Automation）

- 校验定时任务调度变更、规范化调度更新（#1612/#1610）。
- 日历视图渲染定时任务的每日活跃窗口（#1608）。
- automation Skill v1.0.12 → v1.0.13。

### 新功能

- 渠道预设新增 **Qwen3.7 Flash**（#1617）。
- Windows：Agent 状态系统通知与托盘交互优化（完成/报错/待审批提醒）。
- 会话搜索：模糊匹配与结果排序优化（#1586）。

### 修复

- 语音输入：麦克风授权后恢复捕获、听写前等待捕获就绪（#1614/#1603/#1590）。
- 上下文压缩前的过程消息折叠（#1615）；旧的排队运行折叠（#1607）；排队消息后台正确派发（#1594）；移除排队消息 toast（#1582）。
- Markdown：富文本编辑保留 Mermaid 预览（#1595）、修复标签式加粗渲染（#1598）。
- OpenCode Go Vision Relay 模型路由（#1609）；Windows Ctrl+B 快捷键冲突（#1593）。

## 二开保留项（fork-local）

- 发布渠道指向 ai-jiaqian/Proma（electron-builder publish.owner、github-release-service、AboutSettings、安装脚本）。
- 未签名构建守卫；不注入 `Made-with: Proma` 提交标识。
- 版本号走 `-jq.N` 轨道。

## 安装 / 更新

macOS：

```bash
curl -fsSL https://raw.githubusercontent.com/ai-jiaqian/Proma/main/scripts/install.sh | bash
```

或在 Proma 会话里对 Agent 说「更新」。
