# v0.17.40-jq.1 发版说明（2026-08-16）

> 基线：upstream/main `f4af65f8`（官方 package 版本 0.17.40，官方 release 尚未打 tag；提前同步）。
> 同步范围：自官方 v0.17.26（`db94285a`）起 39 个提交。fork 自 v0.17.26-jq.1 起无自有提交，本版全部为同步官方内容。

## 同步官方（v0.17.26 → f4af65f8）

### 架构变化（升级后值得留意）

- **Pi Agent runtime 迁入 Electron utility process**（#1678）：Agent 运行时在独立工具进程中托管，主进程更稳；这是本批最大的架构变化。
- **Adopt Pi-native agent runtime**（#1638）：运行时进一步向 Pi 原生收敛，与 fork 的 Pi-only 基线同向。
- Pi runtime 升级至 **0.84.2**（#1662）。

### 性能与流畅性（本批重头）

- 流式渲染优化三连：优化 agent 流式渲染（#1655）、多会话流式与渲染（#1671）、**消除流式期间逐帧全量 markdown 重解析**（#1673）。
- 会话/历史切换动画与顺滑（#1674/#1664/#1648）；发送消息时防闪屏（#1672）。
- 浏览器面板全局卡顿与面板过渡优化（#1663）；切换会话时隐藏过期视图（#1669）。
- 输入框渲染隔离 usage 更新（#1660）、编辑器更新时保持输入焦点（#1668）。
- 长会话性能退化修复（#1627）；渲染 perf 两连（#1632/#1633）。

### 受管浏览器

- **跨标签跟随 Agent**（#1641）：Agent 操作指定标签时该标签成为会话面板选中标签；in-app-browser Skill 升至 **1.0.15**（fork 此前补的 SPA 内部滚动指引已被上游原生吸收，fork 本地增补退役）。

### Skills

- **PDF Skill 大精简 → 2.0.0**：删除 1800+ 行脚本，改为 MarkItDown 优先的只读路由。

### 新功能

- 渠道预设新增 **GLM-5.3**（#1658）、**火山方舟 Doubao Seed 2.1**（#1630）。

### 修复

- thinking 块闪烁两连修（#1639/#1640）；空流式轮次保留 agent 头部（#1656）；流式期间保持模型身份可见（#1652）。
- 后台标签排队消息重复两连修（#1649 + deduplicate delegated live user messages）。
- 会话标题自动重命名失败回退兜底重命名（#1646/#1661）。
- Agent 工具调用展示改进（#1665）、工具控件与描述对齐（#1666）；Revert 进程紧凑显示（#1657）。
- 引用弹窗稳定（#1670）、滚动条宽度调优（#1667）、侧边栏 tooltip 自然消失（#1680）。
- 消息 channel 身份（#1625）。

## 验证

- typecheck 6 包全过；关键与本批受影响测试 107 pass / 0 fail；build 成功（CLI 61MB + Agent Island helper + EventKit addon，1 个既有 @available warning 无害）。
- fork-local 覆盖全部在位：builder publish.owner=ai-jiaqian、github-release-service owner、AboutSettings×3、release.yml 未签名守卫、README fork 入口、无 Made-with 标识。
