# Proma fork v0.13.19-jq.1

> 基线：官方 v0.13.16 及其后续提交（upstream `978c227a`，package.json version 已 bump 至 0.13.19）。本版为「跟随官方发版」——同步官方 v0.13.15 → 最新的 14 个提交，fork 自有改动无新增。
> 不签名、手动下载安装。

## 同步官方（14 提交）

**新功能 / 增强**
- 委派子会话 `resultSummary` 字符上限从 12K 提高到 50K，返回结果更完整（#955）
- 支持 streamable HTTP MCP 别名（#952）
- 自动任务把 `sourceAutomationId` 透传给委派子会话，委派链路可追溯来源（#943）

**修复 / 稳定性**
- 透传 SDK `result.errors`，并为 `error_during_execution` 接上自动重试（#961）
- 渠道连接测试错误规范化（status-first），DeepSeek/Kimi 等报错更准确（#960）
- DeepSeek/Kimi 等 Anthropic 协议渠道 Chat 模式补全 `/v1`（#951）
- 提升 agent fork 稳定性（#945）
- 修复委派 resume 恢复（#944）

**UI / 编辑器打磨**
- agent 文件 chip 与行内代码样式对齐（#964）
- 输入框光标可见性改善（#948）
- 文件树选中高亮细化（#947）
- minimap 间距与滚动条宽度调整（#946）
- Markdown 编辑器工具条改为固定行 + 独立滚动，滚动时稳定不动（#953）

**杂项**
- 移除误提交的 `pnpm-lock.yaml` 并在 `.gitignore` 屏蔽（#957）

## fork 自有改动

- 无新功能提交。仅同步上游 + `package.json` version → `0.13.19-jq.1`。
- fork-local 覆盖（electron-builder/github-release-service owner = ai-jiaqian、release.yml 未签名守卫、README 安装段）保持在位，上游本批均未触碰。
